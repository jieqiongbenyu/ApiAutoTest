<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.course">
    <!--登录接口sql-->
    <select id="login" parameterType="com.course.model.User" resultType="Integer">
        select count(*) from user
        where user = #{user}
        and password = #{password}
    </select>

    <!--增加用户接口sql-->
    <insert id="addUser" parameterType="com.course.model.User">
        insert into user(user,password,age,sex,permission,isDelete)
        values(#{user},#(password},#{age},#{sex},#{permission},#{isDelete})
    </insert>

    <!--获取用户信息-->
    <select id="getUserInfo" parameterType="com.course.model.User" resultType="com.course.model.User">
        select * from user
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="null != id and ''!= id">
                AND id = #{id}
            </if>
            <if test="null != user and ''!= user">
                AND user = #{user}
            </if>
            <if test="null != password and ''!= password">
                AND password = #{password}
            </if>
            <if test="null != age and ''!= age">
                AND age = #{age}
            </if>
            <if test="null != sex and ''!= sex">
                AND sex = #{sex}
            </if>
            <if test="null != permission and ''!= permission">
                AND permission = #{permission}
            </if>
            <if test="null != isDelete and ''!= isDelete">
                AND isDelete = #{isDelete}
            </if>
        </trim>
    </select>

    <!--更新删除用户信息-->
    <update id="updateUserInfo" parameterType="com.course.model.User">
        update user
        <trim prefix="SET" suffixOverrides=",">
            <if test="null != user and '' != user">
                SET user = #{user},
            </if>
            <if test="null != password and '' != password">
                SET password = #{password},
            </if>
            <if test="null != user and '' != user">
                SET user = #{user},
            </if>
            <if test="null != age and '' != age">
                SET age = #{age},
            </if>
            <if test="null != sex and '' != sex">
                SET sex = #{sex},
            </if>
            <if test="null != permission and '' != permission">
                SET permission = #{permission},
            </if>
            <if test="null != isDelete and '' != isDelete">
                SET isDelete = #{isDelete},
            </if>
        </trim>
    </update>
</mapper>